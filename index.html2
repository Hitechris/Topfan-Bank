<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Welcome to Topfan Bank</title>

  <!-- Favicon (optional, can use logo image as icon) -->
  <link rel="icon" type="image/jpeg" href="IMG_2525.jpeg">

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(to bottom right, #003366, #005588);
      color: #ffffff;
      overflow-x: hidden;
    }

    /* Preloader */
    #preloader {
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background-color: #000;
      display: flex;
      justify-content: center;
      align-items: center;
      z-index: 9999;
    }

    #preloader .spinner {
      border: 6px solid #ccc;
      border-top: 6px solid #00bfff;
      border-radius: 50%;
      width: 60px;
      height: 60px;
      animation: spin 1.5s linear infinite;
    }

    @keyframes spin {
      0% { transform: rotate(0deg);}
      100% { transform: rotate(360deg);}
    }

    /* Main container */
    .container {
      text-align: center;
      padding: 60px 20px;
    }

    /* Logo */
    .logo {
      max-width: 140px;
      margin-bottom: 20px;
      animation: fadeIn 2s ease;
    }

    h1 {
      font-size: 2.8rem;
      margin-bottom: 10px;
      animation: fadeInDown 1.2s ease-in-out;
    }

    .slogan {
      font-size: 1.2rem;
      color: #dddddd;
      animation: fadeIn 2s ease;
    }

    /* Buttons */
    .buttons {
      margin-top: 40px;
    }

    .buttons a {
      text-decoration: none;
      display: inline-block;
      margin: 12px;
      padding: 14px 28px;
      font-size: 1rem;
      background-color: #00bfff;
      color: white;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.2);
      transition: all 0.3s ease;
      animation: slideInUp 1s ease;
    }

    .buttons a:hover {
      background-color: #009acd;
      transform: scale(1.05);
    }

    /* About section */
    .about {
      margin-top: 60px;
      max-width: 800px;
      margin-left: auto;
      margin-right: auto;
      color: #f0f0f0;
      font-size: 1rem;
      line-height: 1.7;
      animation: fadeIn 3s ease;
    }

    .about h2 {
      font-size: 1.5rem;
      margin-bottom: 15px;
      color: #00bfff;
    }

    /* Animations */
    @keyframes fadeInDown {
      from {opacity: 0; transform: translateY(-30px);}
      to {opacity: 1; transform: translateY(0);}
    }

    @keyframes fadeIn {
      from {opacity: 0;}
      to {opacity: 1;}
    }

    @keyframes slideInUp {
      from {opacity: 0; transform: translateY(40px);}
      to {opacity: 1; transform: translateY(0);}
    }

    /* Responsive */
    @media screen and (max-width: 600px) {
      h1 {
        font-size: 2rem;
      }

      .buttons a {
        width: 80%;
        padding: 12px;
      }
    }
  </style>
</head>

<body onload="playWelcome(); hidePreloader();">

  <!-- Preloader -->
  <div id="preloader">
    <div class="spinner"></div>
  </div>

  <!-- Main content -->
  <div class="container">
    <!-- Bank Logo -->
    <img src="IMG_2525.jpeg" alt="Topfan Bank Logo" class="logo">

    <!-- Welcome text -->
    <h1>Welcome to Topfan Bank</h1>
    <p class="slogan">Trusted and Secured Online Bank for Your Future</p>

    <!-- Navigation Buttons -->
    <div class="buttons">
      <a href="login.html">Login</a>
      <a href="TopfanMainMenu.html">Dashboard</a>
    </div>

    <!-- Bank Explanation -->
    <div class="about">
      <h2>About Topfan Bank</h2>
      <p>
        Topfan Bank is a modern, secure, and user-centered digital banking platform designed to serve individuals and businesses with simplicity and trust. 
        <br><br>
        We offer comprehensive banking features like savings, checking, transfers, debit/credit card management, statements, and bill payments — all from a powerful and intuitive dashboard.
        <br><br>
        Our system ensures your transactions are encrypted and your data is protected with two-factor authentication and the latest online security standards. 
        <br><br>
        At Topfan Bank, we don’t just store your money — we empower your future. Start banking smarter today.
      </p>
    </div>
  </div>

  <!-- Welcome audio -->
  <audio id="welcomeAudio" autoplay>
    <source src="welcome.mp3" type="audio/mpeg">
    Your browser does not support audio.
  </audio>

  <!-- JavaScript -->
  <script>
    function hidePreloader() {
      setTimeout(() => {
        document.getElementById("preloader").style.display = "none";
      }, 1500);
    }

    function playWelcome() {
      const audio = document.getElementById("welcomeAudio");
      if (audio) {
        audio.play().catch(err => console.warn("Autoplay failed:", err));
      }
    }
  </script>
</body>
</html>
